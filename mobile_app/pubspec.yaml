name: mobile_app
description: "NeoCamo - Cámara profesional iPhone para Windows"
publish_to: 'none'
version: 2.0.0+4

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  permission_handler: ^11.3.1
  camera: ^0.11.0+2
  web_socket_channel: ^3.0.1
  network_info_plus: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
